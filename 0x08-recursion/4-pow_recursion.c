#include "main.h"
/**
 * _pow_recursion - returns the value of x to the power of y
 * @x: an int
 * @y: an int
 * Return: if y is less than 0, return -1. Otherwise return the result
 */
int _pow_recursion(int x, int y)
{
	if (y < 0)
	{
		return (-1);
	}
	else
	{
		return (_pow_recursion(x, y);
	}
}
