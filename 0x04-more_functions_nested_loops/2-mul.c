#include "main.h"
/**
 * mul - Multiplies two integers
 * @x: the first integer to be multiplied
 * @y: the second integer to be multiplied with the first integer
 * Return: the result of the multiplication of integers x and y
 */
int mul(int x, int y)
{
	return (x * y);
}
